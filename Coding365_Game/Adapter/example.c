#include <stdio.h>
// this is a simple program that multiply input by 2
// and output to standard output.
int main()
{
    int a;
    while (1)
    {
        scanf("%d", &a);
        printf("%d\n", a * 2);
    }
    return 0;
}